import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eltech.bengkeljosjis',
  appName: 'bengkel josjis',
  webDir: 'out' // 👈 Pastikan diisi 'out'
};

export default config;